---
description: Research workflow — scout → researcher → summary
---
Use the subagent tool with the chain parameter to execute this workflow:

1. First, use the "scout" agent to locate relevant code for: $@
2. Then, use the "researcher" agent to investigate deeply and produce evidence-backed findings (use {previous} placeholder)

Execute this as a chain, passing output between steps via {previous}.
